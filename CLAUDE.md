# 作業の約束

鉄道運転シミュレータ。狙いと構成は `README.md` に書いてある。ここには**手を動かすときの
決まりごと**だけを置く。

## 変更したら通すもの

```bash
npm run typecheck && npm run lint && npm run format:check && npm test && npm run build
```

一続きで 100 秒ほど。CI（`.github/workflows/ci.yml`）が見るのは typecheck・lint・test・
build で、**整形の確認は入っていない**ので手元で通しておく。

## テストの走らせ方

テストの時間はほぼ全部が「シミュレーションを何秒ぶん積むか」で決まる。`Simulation.step`
は呼び出し側の刻みによらず物理 1kHz で積むので、**費用は走らせた秒数に比例する**
（刻みを粗くしても速くならない）。

- **手を入れた場所に関係するものだけ先に走らせる。** `npx vitest related <ファイル>`、
  またはテストファイルを直に指定する（`npx vitest run packages/core/test/brake.test.ts`）。
  効くのは末端を触ったときで、`render/wayside.ts` なら 1 件も走らずに終わる。逆に
  `packages/core` の奥（`sim/` や `brake/`）はほとんどのテストが辿り着くので、
  `related` でも 40 ファイル・80 秒ほどになり全件と大差ない。全件はコミット前に 1 回。
- **同じ条件の走行は計算し直さない。** 自動運転のテストは同じ 1 本の走行について
  「停止位置は」「衝動は」「保安装置は」と別々に確かめるので、
  `packages/data/test/autoRun.ts` が結果を覚えている。返ってきた `sim` は**読むだけ**に
  すること（続きを `step()` したい試験は `new Simulation(...)` から自分で組む）。
- **重いテストを足したら `vitest.config.ts` の `SLOW_FIRST` に足す。** 並列に走る単位は
  ファイルで、長いファイルを後から始めるとそれがそのまま全体の終了時刻になる。順番の
  話でしかないので、忘れても遅くなるだけで壊れはしない。
- 1 つのファイルが飛び抜けて長くなったら分ける。ファイルの中は並列にならない。

## 見た目の確かめ方

**走らせて現地まで待たないこと。** 倍率（`[` `]`）を上げても待ち時間は消えない。
1 フレームで進む実時間は 0.25 秒ぶんまで、その中の物理も 2 秒ぶんまでと決まっていて、
描画の遅い環境ではそこが頭打ちになる。入口が 2 つある。

- **部品の姿見** — `npm run dev` して `/parts.html`。線路際の部品を実際の路線から組み、
  それが建つ距離程に運転士の目の高さでカメラを置いて並べる。数秒で全部見える。
  `?p=grade` のように絞れる。部品を足したら `apps/debugger/src/parts.ts` の `KINDS` に
  1 行足す。
- **開始位置の指定** — `/?s=2000&v=60&ui=0` で距離程 2000m・60km/h から、画面表示を
  消した状態で始まる。記録の再生では効かない（記録には「どこから走ったか」まで入って
  いるため）。

ヘッドレスで撮るときは Chromium（`/opt/pw-browsers/` 配下）を
`--use-gl=swiftshader --enable-unsafe-swiftshader` で起動する。ソフトウェア描画なので
1 fps 程度しか出ない。キャンバスへの `locator.click()` は返ってこないので、
`page.keyboard` と `page.evaluate(() => el.click())` で操作する。Playwright は依存に
入れていない（要るときだけ入れる）。

## 書き方

- **コメントは日本語で、「なぜそうなっているか」を書く。** 何をしているかはコードが
  言う。実物の鉄道でどうなっているか、なぜその値なのか、どこを間違えやすいかを残す。
- **実物に合わせる。** 寸法は `apps/debugger/src/render/dimensions.ts` に出典つきで
  集約する。標識の文字は**進来する列車が読む面にだけ**入れ、裏は無地の板を背中合わせに
  張る（1 枚の板を両面表示にすると裏から鏡像に見える）。
- **単位は内部を SI に統一する。** 現場単位（km/h・‰・mm・t・kgf/t・kPa）との換算は
  `packages/core/src/units.ts` に集約し、データと表示の境目でだけ使う。
- **`packages/core` は DOM にも three.js にも依存しない。** 描画に要るものは
  `apps/debugger/src/render/` 側で組む。距離程から軌道座標系への変換は `render/frame.ts`
  が唯一の出所。
- **決定論を壊さない。** 記録・再生と状態ハッシュの比較が成り立つよう、入力を増やしたら
  `NEUTRAL_INPUT` と `sameInput`（`packages/core/src/sim/`）にも足す。乱数は
  シード付きのものだけを使う。
