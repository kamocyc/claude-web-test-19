/**
 * 描画に使う実物の寸法。
 *
 * この 1 ファイルに集めてあるのは、3D モデルの形が「それらしく見えるか」ではなく
 * **実物と同じ寸法か**で決まるようにするためである。値はすべて日本の在来線
 * （狭軌 1067mm・直流 1500V・20m 級通勤形電車）の標準値で、単位は m・rad。
 *
 * 主な出典:
 *  - レール断面: JIS E 1101（普通レール）50kgN レール
 *  - まくらぎ: JIS E 1201（PC まくらぎ）3 号、本線 1 級線 44 本 / 25m
 *  - 道床: 在来線 1 級線 まくらぎ下 250mm・肩幅 400mm・のり面 1:1.5
 *  - 架線: シンプルカテナリ トロリ線高さ 5000mm・システム高さ 960mm・
 *          ハンガー間隔 5m・偏位 ±200mm・標準径間 50m
 *  - 信号機: 色灯式 4 灯（5 現示）、灯器レンズ φ250mm、
 *          柱は軌道中心から 1900mm、灯器下端はレール面上 4200mm 以上
 *  - ホーム: 電車専用区間の高さ レール面上 1100mm、縁端は軌道中心から 1600mm
 *  - 車両: 20m 級 4 扉通勤形（連結面間 20000mm・車体幅 2950mm・
 *          床面高さ 1130mm・台車中心間 13800mm・車輪径 860mm）
 *
 * ## 座標の約束
 *
 * `TrackFrame.position` は**レール頭頂面（走行面）の軌道中心**にある。したがって
 * ここでの「高さ」はすべてレール面を 0 とした値で、レールそのものは -0.153..0 を占める。
 */

/** レール（50kgN・JIS E 1101） */
export const RAIL = {
  /** 高さ */
  height: 0.153,
  /** 底部幅 */
  baseWidth: 0.127,
  /** 頭部幅 */
  headWidth: 0.065,
  /** 腹部厚 */
  webThickness: 0.015,
  /** 定尺レール長（継目の位置に使う） */
  standardLength: 25,
} as const;

/** まくらぎ（PC まくらぎ 3 号相当） */
export const SLEEPER = {
  /** 長さ */
  length: 2.0,
  /** 上面の幅 */
  topWidth: 0.22,
  /** 底面の幅 */
  bottomWidth: 0.26,
  /** 高さ（レール座） */
  height: 0.19,
  /** 敷設間隔 [m]。本線 1 級線の 44 本 / 25m */
  spacing: 25 / 44,
} as const;

/** 道床と路盤 */
export const BALLAST = {
  /** まくらぎ下の道床厚 */
  depth: 0.25,
  /** 道床肩幅（まくらぎ端から） */
  shoulder: 0.4,
  /** のり面の勾配（水平 / 垂直） */
  slope: 1.5,
  /** 路盤肩の幅（道床のり尻から） */
  formationShoulder: 0.6,
  /** 路盤面から地表までの高さ（盛土） */
  embankment: 0.6,
  /** 盛土のり面の勾配 */
  embankmentSlope: 1.8,
} as const;

/** 架線（シンプルカテナリ・直流 1500V） */
export const CATENARY = {
  /** トロリ線高さ（レール面上） */
  contactHeight: 5.0,
  /** システム高さ（支持点でのトロリ線とちょう架線の間隔） */
  systemHeight: 0.96,
  /** 標準径間（支持点の間隔） */
  span: 50,
  /** ハンガーの間隔 */
  hangerSpacing: 5,
  /** 偏位（支持点で軌道中心から左右へ振る量） */
  stagger: 0.2,
  /** ちょう架線の径間中央でのたるみ */
  sag: 0.8,
  /** 電化柱の建植位置（軌道中心から。ホームと反対側に立てる） */
  poleOffset: 3.0,
  /** 電化柱の高さ（レール面上） */
  poleHeight: 8.0,
  /** トロリ線の直径（GT-SN 110mm² 相当） */
  contactDiameter: 0.0123,
  /** ちょう架線の直径（CdCu 65mm² 相当） */
  messengerDiameter: 0.0105,
} as const;

/**
 * 色灯式信号機（4 灯式 = 5 現示）。
 *
 * 灯器は上から 黄・緑・黄・赤 の 4 個で、
 * 停止 R = 赤、警戒 YY = 上下の黄 2 灯、注意 Y = 下の黄、
 * 減速 YG = 上の黄 + 緑、進行 G = 緑、という組み合わせで 5 現示を出す。
 */
export const SIGNAL = {
  /** レンズの直径 */
  lensDiameter: 0.25,
  /** 灯器（フードを含む外径） */
  unitDiameter: 0.33,
  /** フードの長さ */
  hoodLength: 0.3,
  /** 灯器の上下間隔 */
  unitPitch: 0.36,
  /** 背板の幅 */
  boardWidth: 0.5,
  /** 柱の直径 */
  mastDiameter: 0.165,
  /** 柱の建植位置（軌道中心から。進行方向左側） */
  offset: 2.2,
  /** 最下段（赤）の灯器の中心高さ（レール面上） */
  bottomLampHeight: 4.35,
} as const;

/** プラットホーム（電車専用区間） */
export const PLATFORM = {
  /** 高さ（レール面上） */
  height: 1.1,
  /** 縁端の軌道中心からの距離 */
  edgeOffset: 1.6,
  /** 幅 */
  width: 8.0,
  /** 点字ブロックの縁端からの位置と幅 */
  tactileOffset: 0.8,
  tactileWidth: 0.3,
  /** 上屋の高さ（ホーム面上）と柱の間隔 */
  roofHeight: 3.6,
  roofPitch: 5.0,
} as const;

/**
 * 20m 級 4 扉通勤形電車。
 *
 * 車体は腰部で最大幅 2950mm、床下へ向かって 2800mm へ絞る（裾絞り）。
 * 屋根は肩に R を持ち、中央がレール面上 3655mm になる。
 */
export const CAR = {
  /** 連結面間距離 */
  couplerLength: 20.0,
  /** 車体長（妻面間） */
  bodyLength: 19.5,
  /** 車体幅（腰部の最大幅） */
  bodyWidth: 2.95,
  /** 裾（床下）の幅 */
  skirtWidth: 2.8,
  /** 床面高さ（レール面上） */
  floorHeight: 1.13,
  /** 幕板上端（屋根の肩が始まる高さ） */
  shoulderHeight: 3.24,
  /** 屋根の高さ（レール面上・中央） */
  roofHeight: 3.655,
  /** 屋根の肩の丸み */
  roofRadius: 0.45,
  /** 台枠下端（床下機器の取付面） */
  underframeHeight: 0.94,
  /** 台車中心間距離 */
  bogieSpacing: 13.8,
  /** 固定軸距 */
  wheelbase: 2.1,
  /** 車輪径 */
  wheelDiameter: 0.86,
  /** 側扉の幅と高さ */
  doorWidth: 1.3,
  doorHeight: 1.85,
  /** 側扉の中心間隔（20m 4 扉の標準） */
  doorPitch: 4.82,
  /** 側窓の高さと下端（床面上） */
  windowHeight: 0.86,
  windowSill: 0.9,
  /** 運転室の長さ（先頭車の妻面から仕切りまで） */
  cabLength: 2.05,
  /** 運転士の着座位置（車体中心から左へ） */
  driverOffset: 0.7,
  /** 運転士の目の高さ（床面上） */
  eyeHeight: 1.2,
  /** パンタグラフの折りたたみ高さ（レール面上） */
  pantographFolded: 4.14,
} as const;

/**
 * 客室の内装寸法（20m 級 4 扉通勤形）。
 *
 * 車内の広さを決めているのは、車体の内法（外板の内側）と天井の高さ、そして
 * 座席の奥行きである。ロングシートの通勤形では、
 *
 *  - **内法幅 2790mm** — 車体幅 2950mm から側構体の厚さ（片側 80mm）を引いた寸法。
 *  - **座席の奥行き 430mm・座面高さ 430mm** — 立客の通路を広く取るため、
 *    クロスシート（奥行き 800mm 超）よりずっと浅い。
 *  - **通路幅 1930mm** — 上の 2 つの引き算で決まる。ここに立客が入る。
 *  - **天井高さ 2300mm** — 床面から。中央の平天井部分で、冷房の吹き出し口と
 *    ラインデリアがここに並ぶ。
 *  - **吊り革の高さ 1630mm** — 床面から握り手の下端まで。身長 155cm の人が
 *    手を伸ばして届く高さに揃えてある。
 *
 * 出典: JIS E 7103（鉄道車両 — 旅客車の車体設計通則）、および 20m 級通勤形電車の
 * 一般的な諸元。
 *
 * ## 車内を歩くときに効く寸法
 *
 * 一人称で車内を歩くと、**通路のどこに立てるか**が寸法そのものになる。座席の前面
 * （けこみ）から通路側へ出られないので、通路の半幅は `width/2 − seatDepth` = 965mm。
 * 側扉の前だけは座席が無いので内壁まで寄れる。天井高さ 2300mm に対して立客の目は
 * 床上 1600mm なので、荷棚（1750mm）は目のすぐ上を通り、吊り革（1630mm）は
 * ちょうど目の高さに来る — 実際の通勤形の車内で、吊り革が視界を横切って見える
 * のはこの寸法関係による。
 */
export const INTERIOR = {
  /** 内法幅（側構体の内側どうし） */
  width: 2.79,
  /** 側構体の厚さ（外板の内面から内張りまで）。内法幅と車体幅の差の半分。 */
  wallThickness: 0.08,
  /** 天井の高さ（床面上・中央の平天井部） */
  ceilingHeight: 2.3,
  /** 側の天井の高さ（肩の丸みが始まるところ） */
  ceilingSideHeight: 2.05,
  /** ロングシートの奥行き */
  seatDepth: 0.43,
  /** ロングシートの座面高さ（床面上） */
  seatHeight: 0.43,
  /** 背ずりの上端（床面上） */
  seatBackHeight: 0.79,
  /** 1 人あたりの掛け幅（JIS E 7103 の 430mm を割り付けた実寸） */
  seatPitch: 0.46,
  /** 吊り革の握り手の下端（床面上） */
  strapHeight: 1.63,
  /** 荷棚の高さ（床面上） */
  rackHeight: 1.75,
  /** 荷棚の奥行き */
  rackDepth: 0.36,
  /** 立客用の縦手すり（スタンションポール）の径 */
  poleDiameter: 0.032,
  /** 貫通路（妻面の通り抜け）の幅と高さ */
  gangwayWidth: 1.2,
  gangwayHeight: 1.9,
  /** 車端の妻面が車体端から入っている量（連結面の隙間の内側） */
  endWallInset: 0.1,

  // --- 座席まわり ---
  /** 背ずりの傾き（鉛直から後ろへ）[rad]。通勤形のロングシートは 12 度ほど。 */
  seatBackTilt: (12 * Math.PI) / 180,
  /** 座面の厚さ（モケットを張った座ぶとんの厚み） */
  seatCushion: 0.09,
  /** けこみ（座席下の面）が座面前縁より奥に入っている量 */
  seatToeSpace: 0.09,
  /**
   * 座席下のヒーター（電気暖房器）の高さ。けこみ板の下側にあり、
   * 吹き出しのスリットがこの高さに並ぶ。
   */
  heaterHeight: 0.22,
  /**
   * 袖仕切り（座席の端の大きな仕切り板）の高さと厚さ。
   * 立客が座っている人へもたれ掛からないよう、近年の通勤形は肩の高さ
   * （床上 1300mm 前後）まで立ち上げた大形のものになっている。
   */
  armPartitionHeight: 1.32,
  armPartitionThickness: 0.045,
  /** 袖仕切りの前後方向の張り出し（座席前縁より通路側へ出る量） */
  armPartitionDepth: 0.52,

  // --- 吊り革 ---
  /**
   * 吊り革棒（つり手棒）の高さと、車体中心からの左右位置。
   * 座席前縁のやや通路側に通し、座っている人の頭上に来ないようにする。
   */
  strapBarHeight: 1.9,
  strapBarOffset: 0.72,
  /** 吊り革棒の直径 */
  strapBarDiameter: 0.032,
  /** 吊り革の前後の間隔 */
  strapPitch: 0.44,
  /** 握り手（三角形）の外形の高さと幅、握る棒の太さ */
  strapGripHeight: 0.13,
  strapGripWidth: 0.115,
  strapGripBar: 0.022,
  /** 吊り革の帯（棒から握り手まで）の幅と厚さ */
  strapBeltWidth: 0.026,

  // --- 荷棚（網棚）---
  /** 荷棚の受けパイプの直径 */
  rackTubeDiameter: 0.028,
  /** 荷棚の縁（通路側）の立ち上がり */
  rackLip: 0.05,

  // --- 側扉（両開き）---
  /**
   * 鴨居（扉の上の内張り）の下端。扉高さ（1850mm）にわずかな戸当たりの
   * 余裕を足した高さで、ここに案内表示器と扉上の広告が入る。
   */
  doorHeaderHeight: 1.88,
  /** 戸袋（開いた戸が納まる部分）の幅。片側 1 枚ぶん。 */
  doorPocketWidth: 0.66,
  /** 扉の内側の面が内壁より通路側へ出ている量 */
  doorInset: 0.03,
  /** 扉の窓の幅・高さと下端（床面上） */
  doorWindowWidth: 0.46,
  doorWindowHeight: 0.72,
  doorWindowSill: 0.95,
  /** 扉の敷居（ドアレール）の幅 */
  doorSillWidth: 0.09,
  /** 車内案内表示器（扉上の LCD／LED）の幅と高さ */
  displayWidth: 0.68,
  displayHeight: 0.17,

  // --- 貫通路・妻面 ---
  /** 妻面（車端の仕切り壁）の厚さ */
  endWallThickness: 0.06,
  /** 貫通扉の幅（片開き 1 枚） */
  gangwayDoorWidth: 0.72,
  /** 貫通扉の窓の幅・高さと下端（床面上） */
  gangwayWindowWidth: 0.42,
  gangwayWindowHeight: 0.95,
  gangwayWindowSill: 0.85,
  /** 幌（内側から見た通路）の内法幅・高さ */
  bellowsWidth: 1.3,
  bellowsHeight: 1.95,
  /** 連結面の隙間（車体長 19500mm と連結面間 20000mm の差） */
  carGap: 0.5,

  // --- 天井 ---
  /** 平天井部の幅（この外側が肩へ向かって下がる） */
  ceilingFlatWidth: 1.6,
  /** 照明（カバー付き蛍光灯）の幅と、車体中心からの左右位置 */
  lightWidth: 0.22,
  lightOffset: 0.5,
  /** ラインデリア（横流ファン）の吹き出し口の幅と位置 */
  lineFlowWidth: 0.16,
  lineFlowOffset: 0.75,
  /** 冷房の吹き出し口（天井中央のスリット）の幅 */
  coolerDuctWidth: 0.34,

  // --- 立っている人 ---
  /**
   * 立客の目の高さ（床面上）。日本人成人の平均身長 1690mm に対する眼高。
   * 車内を歩くモードのカメラはこの高さに置く。
   */
  eyeHeight: 1.6,
  /** 当たり判定に使う体の半径（肩幅の半分に少し余裕を見た値） */
  bodyRadius: 0.24,
} as const;

/**
 * 運転室の内装寸法（20m 級 4 扉通勤形）。
 *
 * ここだけは**運転士の目を原点**にした値で持つ。運転台の内装はカメラの子として
 * 取り付けるため（そうしないと車体動揺で内装まで揺れて、窓の外だけが揺れる
 * という実際の見え方にならない）、目からの相対位置で書いておくほうが素直である。
 * 軸はカメラ局所系（-Z が前方・+X が右・+Y が上）。
 *
 * 床は `CAR.floorHeight` にあり、着座した目はその上 `CAR.eyeHeight` = 1200mm。
 * 運転室の長さは `CAR.cabLength` = 2050mm で、目は前面から 1550mm のところにある。
 * 運転士は車体中心から左へ 700mm（`CAR.driverOffset`）に座るので、左の内壁は
 * 700mm、右（助士側）の内壁は 2100mm 先になる。
 *
 * 出典: 20m 級通勤形電車の運転室配置の一般的な諸元。机（主幹制御器を載せる面）
 * 床上 780mm、計器盤上端 1040mm、前面窓下端 980mm・上端 1950mm は、
 * 「机に肘を置いた高さでハンドルを握り、盤の上端が窓の下枠を越えない」という
 * 運転室の設計条件そのものである。
 */
export const CAB_DESK = {
  /** 前面窓のガラス面（目から前方へ） */
  windshieldZ: -1.55,
  /**
   * 前面窓の下端・上端（床上 980mm 〜 1950mm を目の高さ 1200mm から測った値）。
   * 下端が目より 8 度下にあるので、15m ほど先の線路まで見下ろせる。
   * ここを上げると近くの軌道が見えなくなり、停止位置合わせができなくなる。
   */
  windowBottom: -0.22,
  windowTop: 0.75,
  /** 前面窓の左端・右端（中桟で 2 枚に分かれる。運転士側が広い） */
  windowLeft: -0.68,
  windowRight: 1.9,
  pillarLeft: 0.76,
  pillarRight: 1.0,
  /** 運転室の後ろの仕切り（前面から 2050mm） */
  partitionZ: 0.5,
  /** 天井（床上 2200mm） */
  ceilingY: 1.0,
  /** 床（着座した目の高さぶんだけ下） */
  floorY: -CAR.eyeHeight,
  /** 左右の内壁（左 = 運転士側 700mm、右 = 助士側 2100mm） */
  leftWallX: -0.7,
  rightWallX: 2.1,
  /** 机（ハンドルを載せる面。床上 780mm）の高さと前後の位置 */
  deskY: -0.42,
  deskFrontZ: -1.4,
  deskBackZ: -0.72,
  /** 計器盤の傾き（鉛直から運転士側へ倒す角）と高さ */
  panelTilt: (25 * Math.PI) / 180,
  panelHeight: 0.3,
  /** 速度計の文字板の直径（φ170mm）と圧力計・電流計の直径（φ100mm） */
  speedDialDiameter: 0.17,
  smallDialDiameter: 0.1,
  /** 運転士側の側窓（下降窓）の開口 */
  sideWindowFront: -1.45,
  sideWindowBack: -0.62,
  sideWindowBottom: -0.08,
  sideWindowTop: 0.5,
  /** 運転席の椅子の座面高さ（床上 480mm）と背もたれ上端 */
  seatHeight: 0.48,
  seatBackTop: 1.05,
} as const;

/** ATS 地上子（ATS-SN / ATS-P。まくらぎの上、軌道中心のやや左に置く） */
export const BEACON = {
  length: 0.66,
  width: 0.36,
  height: 0.13,
  /** 軌道中心からの偏り */
  offset: -0.28,
} as const;

/** 電気転てつ機（NS 形相当） */
export const POINT_MACHINE = {
  length: 1.0,
  width: 0.45,
  height: 0.42,
  /** 軌道中心からの距離（トングレール先端の脇） */
  offset: 2.0,
} as const;

/**
 * 橋りょう。
 *
 * 桁の高さ・支間は路線データが持っている（音を決める寸法でもあるため）ので、
 * ここにあるのは形式ごとに決まっている「見た目の寸法」だけである。
 */
export const BRIDGE = {
  /** 主桁の中心間隔（2 主桁のプレートガーダー橋） */
  girderSpacing: 1.8,
  /** 腹板の厚さ（描画上は実物より厚く見せないと見えない） */
  webThickness: 0.05,
  /** 上下フランジの幅と厚さ */
  flangeWidth: 0.52,
  flangeThickness: 0.05,
  /** 桁の上面（まくらぎの下）の高さ */
  deckTop: -0.35,
  /** トラスの主構の中心間隔（下路。列車がこのあいだを通る） */
  trussSpacing: 4.4,
  /** 弦材・斜材の太さ */
  memberSize: 0.32,
  /** 橋脚の幅（線路方向）と長さ（線路直角方向） */
  pierWidth: 1.6,
  pierLength: 4.2,
  /** 高欄（橋の上の手すり）の高さと軌道中心からの位置 */
  parapetHeight: 1.05,
  parapetOffset: 2.4,
  /** 桁下の余裕（谷底から桁の下端まで） */
  clearance: 2.5,
} as const;

/**
 * 踏切。
 *
 * 第 1 種踏切道（警報機と遮断機を備えるもの）の寸法。警報機は道路の両側に建ち、
 * 遮断桿は道路の半分を塞ぐ（両側から挟んで全幅を塞ぐ）。
 */
export const CROSSING = {
  /** 道路が線路の左右へ伸びる長さ */
  roadLength: 30,
  /** 舗装の厚さ */
  pavementThickness: 0.02,
  /** 踏切板の上面（レール頭頂面よりわずかに低い） */
  panelTop: -0.02,
  /** 警報機の柱の高さと直径 */
  postHeight: 3.3,
  postDiameter: 0.12,
  /** 警報灯の直径と間隔 */
  lampDiameter: 0.3,
  lampSpacing: 0.55,
  /** 警報灯の高さ（地面から） */
  lampHeight: 2.9,
  /** 警報機の軌道中心からの距離 */
  postOffset: 3.5,
  /** 遮断桿の高さ（下りきったとき）と太さ */
  barrierHeight: 0.85,
  barrierDiameter: 0.09,
  /** 遮断桿の根元の軌道中心からの距離 */
  barrierOffset: 3.9,
} as const;

/**
 * トンネル（電化区間）の内空断面。
 *
 * 架線を張るため、非電化より高い断面を取る。側壁は起拱線まで垂直、
 * その上がアーチになる。
 *
 * **単線**は半径 `arcRadius` の真円アーチで、天端はレール面上 5400mm。
 * **複線**は線路中心間隔（在来線 3800mm）ぶんだけ横に広く、半幅は
 * 3800/2 + 2500 = 4400mm になる。ここを真円で立ち上げると天端が
 * レール面上 7300mm まで上がり、掘削量が跳ね上がってしまうので、実物と
 * 同じく**扁平なアーチ**（3 心円。ここでは楕円で代える）にして
 * 起拱線から `doubleArchRise` だけ上げ、天端をレール面上 6200mm に収める。
 */
export const TUNNEL = {
  /** 単線のアーチ半径（= 軌道中心から側壁までの内空半幅） */
  arcRadius: 2.5,
  /** 複線断面での起拱線から天端までの高さ（扁平アーチ） */
  doubleArchRise: 3.3,
  /** 起拱線の高さ（レール面上） */
  springLine: 2.9,
  /** 側壁の下端（レール面から下へ） */
  invert: -0.75,
  /** 覆工の厚さ */
  liningThickness: 0.4,
  /** 待避坑の間隔 */
  refugePitch: 50,
} as const;
